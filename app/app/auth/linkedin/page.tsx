"use client";
import { LinkedInCallback } from "react-linkedin-login-oauth2";

const LinkedInCallbackPage = () => {
  // This component handles the 'code' in the URL automatically
  return <LinkedInCallback />;
};

export default LinkedInCallbackPage;
